Make the storing of variables
Make class declaration (user defined class and object)
Manage self super and #
Manage new


User defined classes
When sending a message the environment is a new environment(local to the message execution) created in the beginning of the execution of the message
	+ the parameters (the objects in the message are added in the local environment )
	+ the class environment stored in the PostaObject (where we cannot add others varibles that the one defined before) (add a flag to the environment)
		